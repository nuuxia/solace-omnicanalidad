<li
  class="px-4 border-l-4 mb-1 <%= current_page?(url) ? 'border-n-brand' : 'border-transparent' %>"
>
  <% text_class_name = current_page?(url) ? 'text-n-brand bg-slate-25' : 'text-slate-800' %>
  <%= link_to(url, class: text_class_name + " -ml-1 focus:outline-none cursor-pointer flex items-center px-2 py-1.5 text-slate-800 cursor-pointer hover:text-n-brand hover:bg-slate-25 rounded-lg") do %>
    <svg width="16" height="16"><use xlink:href="#<%= icon %>" /></svg>
    <span class="ml-2 text-sm"><%= label %></span>
  <% end %>
</li>
